VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
# The existing map integration is separate from the backend API.
VITE_GOOGLE_MAPS_API_KEY=
# Only for the dedicated localhost pilot; never for a production build.
VITE_LOCAL_PILOT_NO_EMAIL=false
